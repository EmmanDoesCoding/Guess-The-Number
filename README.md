# Guess-The-Number
A classic logic-based game where the user tries to guess a randomly generated number within a specific range.
This repository serves as a cross-language implementation, featuring the same game logic written in both Java and Python.

🚀 Features
• Cross-Platform: Works on any machine with Java or Python installed.
• Interactive UI: Simple command-line interface with real-time feedback.
• Dynamic Feedback: Tells the player if their guess is "too high" or "too low."
• Score Tracking: Keeps track of how many attempts it took to find the number.
